services:
  - type: web
    name: riddim-vault
    env: node
    plan: free
    buildCommand: npm install && npm run build && cd server && npm install
    startCommand: cd server && node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_PIN
        value: riddim140
